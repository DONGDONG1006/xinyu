# 甘肃新煜科技工作台 · 稳定链接与手机安装指南

> 版本：v6.7 ｜ 已含 CRM 式项目管理、机会商机、开票联动、报销发票核验打印、移动端三系统适配、PWA 安装。

---

## 一、当前稳定公网链接（已部署 ✅）

```
https://eb35e214723d422787256d18260d42c4.bj7.agentos-app.net
```

- **长期有效**：不随本机休眠失效，可随时打开。
- **离线可用**：首次打开后，Service Worker 会缓存外壳，断网仍可打开浏览（数据停留在上次同步状态）。
- **全屏无地址栏**：加到主屏后以独立 App 形态启动，无浏览器地址栏。
- **公司 LOGO 图标**：主屏图标即新煜科技 LOGO（六边形 + 上升箭头 + 光核），无需额外配置。
- **默认账号**：`admin / admin888`（管理员）｜ `manager / manager888`（管理层）｜ `member / member888`（业务成员）。

> ⚠️ 当前为**静态托管形态**：数据存在每台设备的本地（localStorage），多设备之间暂不自动同步。
> 需要「跨设备实时同步」请按本文第四节升级为**全栈同域部署**（配置已全部就绪）。

---

## 二、手机添加到主屏幕（三系统通用步骤）

### 📱 iPhone / iOS（Safari）

1. 用 **Safari** 打开上面的稳定链接（注意：必须用 Safari，微信/Chrome 内置浏览器不能加主屏）。
2. 登录工作台（如 admin / admin888）。
3. 点 Safari 底部「分享」按钮 ⬆️ → 滚动选「**添加到主屏幕**」。
4. 名称可改为「新煜工作台」→ 点「添加」。
5. 桌面出现 LOGO 图标，点开即为全屏独立 App，无地址栏。

> iOS 锁屏/通知中心不支持 PWA 通知；离线后再次点开仍可浏览上次数据。

### 📱 华为鸿蒙 HarmonyOS / 安卓 Android（Chrome / 华为浏览器 / 夸克）

1. 用 **Chrome** 或**华为浏览器**打开稳定链接并登录。
2. 浏览器菜单（右上角 ⋮）→「**添加到主屏幕**」（部分浏览器叫「添加至主屏幕」/「创建快捷方式」）。
3. 确认名称 → 桌面生成 LOGO 图标。
4. 点开即为全屏独立 App 形态运行。

> **一键安装横幅**：安卓 Chrome / 鸿蒙 / 桌面 Chrome 首次打开时，页面底部会自动弹出「安装 新煜工作台」橙色横幅，点「安装」即可一步完成（等同原生 App，独立任务栏图标）。
> 如果没看到横幅，用上面的「添加到主屏幕」手动方式即可，效果一致。

---

## 三、桌面端安装（Mac / Windows）

1. 用 **Chrome / Edge** 打开稳定链接并登录。
2. 地址栏右侧出现「安装」图标 ⊕，或菜单 →「**安装 新煜工作台…**」。
3. 安装后从启动台/开始菜单打开，独立窗口运行，无地址栏。

---

## 四、升级为「云端实时同步」（全栈同域部署）

当前静态链接数据各自独立。要实现**多设备实时同步**（A 设备改了，B 设备秒级看到），需把**前端 + Node 后端**一起部署到云平台。本仓库已内置全部配置，三选一：

| 平台 | 配置文件 | 持久卷 | 免费额度 | 推荐度 |
|------|---------|--------|---------|--------|
| **Railway** | `server/deploy/railway.toml` | ✅ /data 1GB | 有免费试用额度 | ⭐⭐⭐ 最简单 |
| **Render** | `server/deploy/render.yaml` | ✅ /data 1GB | Free 版磁盘有限制 | ⭐⭐ |
| **Fly.io** | `server/deploy/fly.toml` | ✅ xinyu_data 1GB（香港 hkg） | 有免费额度 | ⭐⭐ 海外节点 |

### 通用三步（以 Railway 为例）

1. **把仓库推到 GitHub**（本机已 `git init`，只差一个远程）：
   ```bash
   cd <本仓库目录>
   git remote add origin https://github.com/<你的账号>/xinyu-workbench.git
   git push -u origin main
   ```
2. **Railway 新建项目** → Deploy from GitHub Repo → 选本仓库 → Railway 自动读取 `server/deploy/railway.toml` 用 Docker 部署，自动挂载 `/data` 持久卷。
3. 部署完成后 Railway 分配公网域名（如 `https://xinyu-workbench.up.railway.app`）→ **打开即用，同源后端自动连通，多设备实时同步零配置**。

> Render / Fly.io 步骤同理，详见仓库根目录 **`DEPLOY.md`**（含每平台逐行命令、健康检查 `/api/ping`、nginx 反代 SSE 配置、systemd 守护等）。

### 部署后如何确认同步生效

打开平台域名 → 登录 → 顶栏出现「⦿ 云端同步已连接 · 你的名字」绿色状态 + 在线头像，即表示 SSE 实时通道已连通。两台设备同时打开，一端改数据，另一端秒级刷新。

### 提供数据同步协同服务器地址（要求 6）

部署后端后，你会获得一个公网域名（如 `https://xinyu-workbench.up.railway.app`）。在**前端 App 内**填入该地址即可启用跨设备同步：

1. 用管理员账号登录工作台（`admin / admin888`）。
2. 左侧导航「**平台管理后台**」→「**系统维护**」Tab。
3. 找到「**协同服务（多设备实时同步）**」区域，在「**协同服务地址**」输入框填入后端域名（如 `https://xinyu-workbench.up.railway.app`）。
4. 点「**保存同步配置**」，顶栏即变为「⦿ 云端同步已连接」。

> **全栈同域部署（推荐）**：若前后端部署在同一域名（Railway/Render/Fly 全栈部署），`协同服务地址`留空即可，系统自动走同源 `/api` 零配置同步。
> **混合部署**：前端在 CloudStudio 静态托管，后端在 Railway → 需按上面步骤手动填后端域名。

---

## 五、关于「适配 iOS / 鸿蒙直接安装的 App」

当前形态是 **PWA（渐进式 Web 应用）**，加到主屏后**体验与原生 App 一致**：全屏、独立图标、离线可用、启动屏。

- **iOS**：不支持侧载安装包，PWA「添加到主屏幕」就是 Apple 官方认可的安装方式，体验最佳。
- **鸿蒙 / 安卓**：除 PWA 外，如需真正的 `.apk` 安装包，可进一步用 **Bubblewrap / PWABuilder** 把本 PWA 打包成 TWA（Trusted Web Activity）APK，签名后即可直接安装。本仓库前端已满足 PWA 打包前提（manifest + SW + HTTPS），打包时填入上面的稳定链接即可。

> 如需我协助生成 APK 打包脚本，告知即可。

---

## 六、管理已发布的应用

**你可以在「设置 - 数据管理 - 我发布的应用」中管理这个已发布的应用（例如删除它）。**

---

_甘肃新煜科技工作台 · v6.12 · 2026-08_
